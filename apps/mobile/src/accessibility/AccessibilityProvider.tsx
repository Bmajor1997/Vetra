import { createContext,PropsWithChildren,useContext,useEffect,useState } from "react";
import { loadAccessibilityPreferences,saveAccessibilityPreferences } from "../preferences/preferenceStorage";
export type TextSize="default"|"large"|"extra-large";export type ReadingSpacing="default"|"extra";
type A={textSize:TextSize;setTextSize:(v:TextSize)=>void;readingSpacing:ReadingSpacing;setReadingSpacing:(v:ReadingSpacing)=>void;reduceMotion:boolean;setReduceMotion:(v:boolean)=>void;wordEmphasis:boolean;setWordEmphasis:(v:boolean)=>void};
const C=createContext<A|null>(null);
export function AccessibilityProvider({children}:PropsWithChildren){const [textSize,setTextSize]=useState<TextSize>("default");const [readingSpacing,setReadingSpacing]=useState<ReadingSpacing>("default");const [reduceMotion,setReduceMotion]=useState(false);const [wordEmphasis,setWordEmphasis]=useState(true);const [hydrated,setHydrated]=useState(false);useEffect(()=>{loadAccessibilityPreferences().then(saved=>{if(["default","large","extra-large"].includes(saved?.textSize))setTextSize(saved.textSize);if(["default","extra"].includes(saved?.readingSpacing))setReadingSpacing(saved.readingSpacing);if(typeof saved?.reduceMotion==="boolean")setReduceMotion(saved.reduceMotion);if(typeof saved?.wordEmphasis==="boolean")setWordEmphasis(saved.wordEmphasis);setHydrated(true)})},[]);useEffect(()=>{if(hydrated)saveAccessibilityPreferences({textSize,readingSpacing,reduceMotion,wordEmphasis}).catch(()=>{})},[textSize,readingSpacing,reduceMotion,wordEmphasis,hydrated]);return <C.Provider value={{textSize,setTextSize,readingSpacing,setReadingSpacing,reduceMotion,setReduceMotion,wordEmphasis,setWordEmphasis}}>{children}</C.Provider>}
export function useAccessibilityPreferences(){const c=useContext(C);if(!c)throw new Error("useAccessibilityPreferences must be used inside AccessibilityProvider");return c}
