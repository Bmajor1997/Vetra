import { PropsWithChildren } from "react"; import { SafeAreaView,ScrollView,StyleSheet,Text,View } from "react-native"; import { useVoticTheme } from "../theme/ThemeProvider"; import { VoticLogo } from "./VoticLogo";
export function Screen({title,children}:PropsWithChildren<{title:string}>){const {theme}=useVoticTheme();return <SafeAreaView style={[s.safe,{backgroundColor:theme.background}]}><ScrollView contentContainerStyle={s.content}><View accessibilityRole="header"><VoticLogo compact /></View><Text accessibilityRole="header" style={[s.title,{color:theme.text}]}>{title}</Text>{children}</ScrollView></SafeAreaView>}
const s=StyleSheet.create({safe:{flex:1},content:{paddingHorizontal:20,paddingBottom:32,gap:16},brand:{fontSize:22,fontWeight:"800",paddingTop:8},title:{fontSize:30,fontWeight:"800",marginTop:4}});
