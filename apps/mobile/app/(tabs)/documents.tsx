import { StyleSheet,Text } from "react-native";import { Card } from "../../src/components/Card";import { Screen } from "../../src/components/Screen";import { useVoticTheme } from "../../src/theme/ThemeProvider";export default function Documents(){const {theme}=useVoticTheme();return <Screen title="Documents"><Card><Text style={[s.h,{color:theme.text}]}>Your library</Text><Text style={{color:theme.mutedText}}>Saved and imported documents will live here. Folder and collection support can be added without redesigning this screen.</Text></Card></Screen>}const s=StyleSheet.create({h:{fontSize:20,fontWeight:"700"}});
