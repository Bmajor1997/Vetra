import * as DocumentPicker from "expo-document-picker";import { File } from "expo-file-system";import { router } from "expo-router";import { useState } from "react";import { Alert,Pressable,StyleSheet,Text,View } from "react-native";import { Card } from "../../src/components/Card";import { extractDocument } from "../../src/api/voticApi";import { Screen } from "../../src/components/Screen";import { canReadLocally,validateImport } from "../../src/documents/importDocument";import { useDocumentLibrary } from "../../src/documents/DocumentLibraryProvider";import { useVoticTheme } from "../../src/theme/ThemeProvider";
export default function Documents(){const {theme}=useVoticTheme();const {documents,addTextDocument,openDocument}=useDocumentLibrary();const [importing,setImporting]=useState(false);
async function addDocument(){setImporting(true);try{const result=await DocumentPicker.getDocumentAsync({type:["text/plain","text/markdown","application/pdf","application/vnd.openxmlformats-officedocument.wordprocessingml.document"],copyToCacheDirectory:true,multiple:false});if(result.canceled)return;const asset=result.assets[0];validateImport({name:asset.name,size:asset.size,uri:asset.uri,mimeType:asset.mimeType});let text:string;if(canReadLocally(asset.name))text=await new File(asset.uri).text();else text=await extractDocument(asset.name,await new File(asset.uri).arrayBuffer());if(!text.trim())throw new Error("This document does not contain readable text.");addTextDocument(asset.name,text);router.push("/reader")}catch(error){Alert.alert("Could not import document",error instanceof Error?error.message:"Votic could not read this document.")}finally{setImporting(false)}}
function open(id:string){openDocument(id);router.push("/reader")}
return <Screen title="Documents"><Pressable accessibilityRole="button" accessibilityLabel="Add document" accessibilityHint="Choose a document from this device" disabled={importing} onPress={addDocument} style={[s.add,{backgroundColor:theme.accent,opacity:importing?.65:1}]}><Text style={s.addText}>{importing?"Importing…":"Add document"}</Text></Pressable>{documents.length===0?<Card><Text style={[s.h,{color:theme.text}]}>Your library</Text><Text style={{color:theme.mutedText}}>No documents yet. Import a TXT, Markdown, PDF, or Word file to start listening.</Text></Card>:<View style={s.list}>{documents.map(doc=><Pressable key={doc.id} accessibilityRole="button" accessibilityLabel={"Open "+doc.title} accessibilityHint={doc.progress?"Resume listening":"Open in Votic reader"} onPress={()=>open(doc.id)}><Card><Text style={[s.h,{color:theme.text}]}>{doc.title}</Text><Text style={{color:theme.mutedText}}>{doc.sourceName}</Text><View accessibilityRole="progressbar" accessibilityValue={{min:0,max:100,now:Math.round(doc.progress*100)}} style={[s.track,{backgroundColor:theme.border}]}><View style={[s.fill,{backgroundColor:theme.accent,width:`${doc.progress*100}%` as `${number}%`}]}/></View><Text style={{color:theme.mutedText}}>{doc.progress?Math.round(doc.progress*100)+"% complete":"Ready to listen"}</Text></Card></Pressable>)}</View>}</Screen>}
const s=StyleSheet.create({add:{minHeight:50,borderRadius:14,justifyContent:"center",alignItems:"center"},addText:{color:"#FFF",fontSize:16,fontWeight:"800"},h:{fontSize:20,fontWeight:"700"},list:{gap:12},track:{height:6,borderRadius:3,overflow:"hidden"},fill:{height:"100%"}});
